<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Bật log DEBUG cho Spring Framework -->
    <logger name="org.springframework" level="DEBUG" />

    <!-- Bật log DEBUG cho toàn bộ Jackson -->
    <logger name="com.fasterxml.jackson" level="DEBUG" />

    <!-- Bật log DEBUG riêng cho deserialization -->
    <logger name="com.fasterxml.jackson.databind.deser" level="DEBUG" />

    <root level="INFO">
        <appender-ref ref="STDOUT" />
    </root>

</configuration>
